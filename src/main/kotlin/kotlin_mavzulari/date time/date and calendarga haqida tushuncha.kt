package `date time`

fun main() {
    // qaysi oy qaysi kun va qaysi sana  va soat minut sekun di bn chiqarib beradi
    /* val date = Date()
     println(date)*/

    // bu calendar
    /*   var date = Date()
       println(date)

       var calendar = Calendar.getInstance()
       calendar.time = date

       println(calendar[Calendar.YEAR])//bu yil ni chiqarib beradi
       println(calendar[Calendar.MONTH])//bu oy ni chiqarib beradi
       println(calendar[Calendar.DAY_OF_WEEK])//bu hafta kunidan boshlab sanedi nechinchi kunligini chiqarib beras*/

    // bu yangi yilgacha nech kun qolganini korish
    /* var date = Date()
     println(date)

     var calendar = Calendar.getInstance()
     calendar.time = date

     println("${365-  calendar[Calendar.DAY_OF_YEAR]}")*/

    // qoshish
    /*  var date = Date()
      println(date)

      var calendar = Calendar.getInstance()
      calendar.time = date

      println("${365 - calendar[Calendar.DAY_OF_MONTH]}")
      calendar.add(Calendar.DAY_OF_MONTH, 1)  // bu yerga qaysinga qoshishni xoxlasayiz shuni berasiz
      */

}